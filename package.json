{
  "name": "mythical-ui",
  "version": "0.0.0",
  "description": "mythicalOS component library — the framework-agnostic core, Preact + React bindings, and the Preact family shell. Built on @mythicalos/tokens.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "scripts": {
    "typecheck": "bun run --filter '*' typecheck",
    "test": "bun test",
    "build": "bun run --filter '*' build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mythicalOS/mythical-ui.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
